# 监控列表

> [参考Google 财经](https://www.google.com/finance/portfolio/watchlist?hl=zh)

| 交易所 | 股票编号 | 中文名 | 一年前收盘价 |  | 一年内涨幅 | 半年前收盘价 | 半年内涨幅 | 3个月前收盘价 | 3个月内涨幅 | 1月前收盘价 | 1月内涨幅 | 1周前收盘价 | 1周内涨幅 | 3日前收盘价 | 3日内涨幅 | 昨日收盘价 | 昨日涨幅 |
| -------- | ------ | ----------- | ---------- | -------- | - | ----------- | - | - | -------- | -------- | -------- | -------- | -------- | -------- | -------- | -------- | -------- |
| 纳斯达克 | AAPL | 苹果 |        |        |        |        |        |        |        |        |        |        |        |             |             |            |          |
| 上交所&深交所 | 指数 | 沪深300 | | | | | | | | | | | | | | | |
| 香港 | 指数 | 恒生指数 | | | | | | | | | | | | | | | |
| 纳斯达克&纽交所 | 指数 | 标准普尔500 | | | | | | | | | | | | | | | |

1. 涨幅用百分比，上涨用带符号正数、红色表示，下跌用带符号负数、绿色表示。
2. 价格需要加上对应的符号，例如美元用$，人民币用￥，港币用 HK$。
3. 数字保留2位小数。
4. 指数类型不用加单位符号。
5. 数据来源于三方API接口查询，查询完后缓存到本地，过期时间为当前剩余时间，即第二天零点失效。
6. 三方接口一般都有限流，因此，前端查询不到数据时，需要定时轮询，后端则是在限流范围内，定时查询三方API过去数据，并存入缓存。
7. 在项目启动时，此时本地缓存没有数据，需要异步调用三方API获取数据。
8. 集成多个 API（如 Yahoo+Polygon + 新浪），某一 API 失败时自动切换。

#### 1. 核心需求（补充细化）

| 维度         | 具体要求                                                     |
| ------------ | ------------------------------------------------------------ |
| 数据维度     | 覆盖 2 类标的（股票、指数），计算 7 个时间维度涨幅（1 年 / 半年 / 3 月 / 1 月 / 1 周 / 3 日 / 昨日） |
| 格式规范     | 1. 涨幅：百分比 + 符号（+ 涨 /- 跌），红色标涨、绿色标跌；<br />2. 价格符号：AAPL ($)、沪深 300 (无)、恒生指数 (无)、标普 500 (无)；<br />3. 所有数字保留 2 位小数 |
| 缓存规则     | 1. 本地缓存存储维度：标的代码、各时间节点收盘价、各周期涨幅；<br />2. 缓存有效期：当日剩余时间（至次日 0 点）；<br />3. 缓存失效后自动触发 API 重新拉取 |
| 接口调用规则 | 1. 项目启动：异步调用 API 初始化缓存；<br />2. 前端：缓存缺失时定时轮询（默认 30 秒 / 次）；<br />3. 后端：限流范围内定时拉取（默认 5 分钟 / 次），避免接口超限 |
| 异常处理     | 1. API 调用失败：缓存保留最后有效数据，页面显示 “数据加载中”；<br />2. 数据缺失：对应单元格显示 “--” |

#### 2. 补充非功能性需求

- 页面适配：支持 PC / 移动端响应式显示；
- 交互体验：数据加载时显示骨架屏，涨幅变化时高亮提示；
- 性能要求：页面首次加载≤3 秒，缓存命中时渲染≤1 秒。



## 财经数据API

| 名称          | 覆盖范围                                                     | 接入方式                                               | 免费额度                                              |
| ------------- | ------------------------------------------------------------ | ------------------------------------------------------ | ----------------------------------------------------- |
| Yahoo Finance | 全球市场                                                     | Python yfinance库（推荐）<br />HTTP 接口（非官方）     | 无严格限制，高频调用（>10 次 / 分钟）可能触发 IP 限制 |
| Alpha Vantage | 美股全量<br />A 股 / 港股（有限支持）<br />全球指数（标普 500 / 纳斯达克） | REST API（需注册免费 API Key）<br />Python/Java/JS SDK | 5 次 / 分钟，500 次 / 日                              |
| AkShare       | A 股全量                                                     | Python 库（完全开源）                                  | 无限制                                                |
| 新浪财经      | A 股全量                                                     | HTTP GET（无需 Key，直接调用）                         | 5 次 / 秒，高频易封 IP                                |
| 腾讯证券      | A 股 / 港股 / 美股                                           | HTTP GET（无需 Key）                                   | 无明确限流                                            |
| Baostock      | A 股全量                                                     | Python 库                                              | 需登录，完全免费                                      |



Spring Boot 同时集成 Yahoo Finance、Alpha Vantage、新浪财经、腾讯证券。

- Spring Boot 3.2.9
- MyBatis-Plus 3.5.14
- H2 2.3.232
- Caffeine 3.1.8

### 表结构

```sql
-- 股票/指数监控表
CREATE TABLE IF NOT EXISTS stock_monitor (
    id BIGINT AUTO_INCREMENT COMMENT '主键ID',
    exchange VARCHAR(50) NOT NULL COMMENT '交易所（如：纳斯达克、上交所&深交所）',
    stock_code VARCHAR(20) NOT NULL COMMENT '股票/指数编号（如：AAPL、指数）',
    stock_name VARCHAR(50) NOT NULL COMMENT '中文名（如：苹果、沪深300）',
    currency_symbol VARCHAR(5) DEFAULT '' COMMENT '币种符号（$、￥、HK$，指数为空）',
    price_1y_ago DECIMAL(10,2) COMMENT '一年前收盘价',
    rise_1y DECIMAL(5,2) COMMENT '一年内涨幅（百分比，带符号）',
    price_6m_ago DECIMAL(10,2) COMMENT '半年前收盘价',
    rise_6m DECIMAL(5,2) COMMENT '半年内涨幅（百分比，带符号）',
    price_3m_ago DECIMAL(10,2) COMMENT '3个月前收盘价',
    rise_3m DECIMAL(5,2) COMMENT '3个月内涨幅（百分比，带符号）',
    price_1m_ago DECIMAL(10,2) COMMENT '1个月前收盘价',
    rise_1m DECIMAL(5,2) COMMENT '1月内涨幅（百分比，带符号）',
    price_1w_ago DECIMAL(10,2) COMMENT '1周前收盘价',
    rise_1w DECIMAL(5,2) COMMENT '1周内涨幅（百分比，带符号）',
    price_3d_ago DECIMAL(10,2) COMMENT '3日前收盘价',
    rise_3d DECIMAL(5,2) COMMENT '3日内涨幅（百分比，带符号）',
    price_yesterday DECIMAL(10,2) COMMENT '昨日收盘价',
    rise_yesterday DECIMAL(5,2) COMMENT '昨日涨幅（百分比，带符号）',
    cache_create_time DATETIME COMMENT '缓存创建时间',
    cache_expire_time DATETIME COMMENT '缓存过期时间（次日0点）',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '数据创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '数据更新时间',
    PRIMARY KEY (id),
    -- 唯一索引：避免重复存储同一标的数据
    UNIQUE INDEX idx_stock_name (stock_name),
    -- 普通索引：优化交易所/编号查询
    INDEX idx_exchange_code (exchange, stock_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='股票/指数监控列表';

-- H2 数据库适配说明：
-- 1. H2 兼容 MySQL 语法，ENGINE=InnoDB 可省略（H2 默认存储引擎）；
-- 2. ON UPDATE CURRENT_TIMESTAMP 需 H2 2.0+ 版本支持（你的 2.3.232 满足）；
-- 3. DECIMAL 精度：price 字段 10位整数+2位小数（适配指数/股票价格范围），rise 字段 5位（如 +999.99%）。
```

